package com.davek.taskapp.tasks

class TasksFragmentTest {

    /**
     * TODO: testing
     *  - Hilt setup:
     *    - CustomTestRunner
     *    - HiltTestActivity
     *    - HiltAndroidRule
     *    - FakeRepository
     *    - @UninstallModules(TaskRepositoryModule::class), then inject FakeRepository for testing
     *    - etc..
     *  - espresso
     *  - etc..
     *
     */
}
